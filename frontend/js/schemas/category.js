/* The Category class is a JavaScript class that has two properties: id and name */
export class Category {
  /**
   * Create a JavaScript constructor function
   * @param id {number} - The id of the user.
   * @param name {string}- The name of the parameter.
   */
  constructor(id, name) {
    this.id = id
    this.name = name
  }
}
